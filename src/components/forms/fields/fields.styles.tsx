import {makeStyles} from '@rneui/themed';

export const useFieldStyles = makeStyles(() => ({
  helperText: {
    paddingTop: 4,
    paddingBottom: 4,
    flexDirection: 'row',
    alignItems: 'center',
  },
}));
