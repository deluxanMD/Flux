import {makeStyles} from '@rneui/themed';

export const useProfileEditStyles = makeStyles(() => ({
  container: {
    flex: 1,
    marginTop: 40,
    paddingLeft: 20,
    paddingRight: 20,
  },
  formContainer: {
    flexGrow: 0.9,
  },
}));
